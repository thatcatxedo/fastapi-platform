apiVersion: v1
kind: Service
metadata:
  name: n8n
  namespace: fastapi-platform
  labels:
    app: n8n
spec:
  type: ClusterIP
  selector:
    app: n8n
  ports:
    - port: 5678
      targetPort: 5678
      name: http
