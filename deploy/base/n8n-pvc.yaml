apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: n8n-data
  namespace: fastapi-platform
  labels:
    app: n8n
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
