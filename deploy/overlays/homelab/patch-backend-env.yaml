apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: fastapi-platform
spec:
  template:
    spec:
      containers:
        - name: backend
          env:
            - name: BASE_DOMAIN
              value: platform.gatorlunch.com
